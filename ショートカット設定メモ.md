# iPhoneの共有シートから予定を取り込む（iOSショートカット）

**2026-08-12 時点。設定は途中まで完了。残り1か所。**

iOS Safari は Web アプリを共有シートに出せない（`manifest.webmanifest` の `share_target` は
**Androidでは効くが、iPhoneでは何も起きない**）。iPhoneでは iOSショートカットを1つ入れるのが唯一の道。

## アプリ側は完成・公開済み（v4.3.0）

- 共有で開かれたら `?text=` を読んで取り込み画面を開く
- **ハッシュタグの `#` で本文が切れない**（`URLSearchParams` を使わず href 末尾まで拾う）
- 渡されたのがページのHTMLなら、`og:description` から**投稿本文だけを取り出す**（`postFromHtml`）
- 日付が拾えずリンクだけのときは、**リンクをメモに入れた「予定を追加」を開く**
- 飾り（`★` `╭━━╮` `📢`）を題名にしない

**ショートカット側にはエンコードも正規表現も要らない。面倒はアプリ側で引き受けてある。**

## 完成形（2アクション）

```
1. 共有シートから入力を受け取る（種類はすべてON。絞るとXの共有に出てこない）
2. URLの内容を取得
     https://publish.x.com/oembed?url=［ショートカットの入力］&omit_script=1
     方法: GET ／ ヘッダは不要
3. URLを開く
     https://takashi33.github.io/oshikatsu-calendar/?text=［URLの内容］
```

**oEmbed は JSON で返るので、ショートカット側に変換も正規表現もヘッダも要らない。**
Xが渡してくる `?s=12&t=…` が付いたままでも通ることを確認済み。

## 行き止まりだった道（同じ穴に落ちないための記録）

**ページのHTMLを取りに行く方式は、iOSでは使えない。**

- `x.com/…/status/…` を `Discordbot` を名乗って取ると、`og:description` に本文が入って返る（**PCでは成功**）
- しかし iOSショートカットでは、返りが **「ファイル」扱い**になる。
  ファイルをURLの文字欄に入れると**空**になる
- 「入力からテキストを取得」で文字に直そうとすると、**「見える文字」だけ**を拾う処理が走る。
  Xのページは本文が `<head>` にあり見える部分は空なので、**結果も空**
- **エラーは一切出ない。** 黙って空が返るだけなので、原因に辿り着くのに何往復もかかった

**プライバシーの許可も必要だった。** ⓘ →「プライバシー」タブ。
共有時に「"推しごとに追加"が1個のXの項目を"x.com"へ送信することを許可しますか？」が出たら
**「常に許可」**（「1度だけ許可」だと毎回聞かれる）。

## 分かっていること（検証済み）

- **Xの共有は、投稿本文を渡さない。リンクだけ。** だから本文を取りに行く必要がある
- **`https://publish.x.com/oembed?url=…` が本文を JSON で返す。** ヘッダも認証も不要
- 組み立てたURLは約2,700文字。Safariの上限には遠く届かない
- ⚠️ **oEmbed の `html` は、本文の `<p>` のあとに「— 著者名 投稿日」が続く。**
  ここを本文に混ぜると**投稿日が予定になってしまう**ので、`<p>…</p>` の中だけを採ること
- **Safariとホーム画面アプリは、保存先が共通**（実機で確認）。
  → ショートカットがSafariで開いても、保存した予定はホーム画面アプリに出る
  → 同時に、**Safariのデータ消去で予定が消えうる**ということでもある

## 依頼者へ渡すときは

**渡すものはアプリのURL 1つだけにする。**
ショートカットは、作成者がiCloudリンクとして共有し、それをアプリの説明画面に埋め込む
（`index.html` の `SHORTCUT_URL`。**空のあいだはボタンを出さず「準備中」と表示する**）。

⚠️ **iCloudリンクは作成者のApple IDに紐づく。消すと配布先の再設定手段が無くなる。**

## 弱点として伝えておくこと

- **Xが本文を配るのをやめたら、自動読み取りは止まる。** そのときはリンクだけが届き、
  「予定を追加」に落ちる。**取り込み済みの予定は影響を受けない**（Xとは無関係に保存されている）
- Xアプリ以外（Safariの告知ページ・LINE・メモ）からの共有は、**この仕組みに依存せず動く**
